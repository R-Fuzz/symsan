# The UCSan tests are not lit tests -- they explore paths under their own Python
# harness rather than compiling and checking one run.  Nothing in this directory
# should be discovered by lit, so it recognises no suffixes here; see README.md.
config.suffixes = []
